export interface Navigation {
    href: number;
    anchorID: string;
    navText: string;
}

/*
TODO: use this interface in Header.tsx
Require a Path to json File
Then Fetch Nav Data from Json into Navigation[]
Then Display Content iteratively
 */